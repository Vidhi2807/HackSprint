// Speech Service Stub
// TODO: Implement Web Speech API / server-side TTS

export const transcribe = async (audioBuffer) => {
  return { text: '' };
};

export const synthesize = async (text) => {
  return { audio: null };
};
